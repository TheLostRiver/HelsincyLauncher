[package]
name = "launcher-kernel-jobs"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true

[lib]
name = "launcher_kernel_jobs"
path = "src/lib.rs"

[dependencies]
launcher-kernel-foundation = { path = "../kernel-foundation" }
serde.workspace = true